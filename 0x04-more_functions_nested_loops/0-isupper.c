#include "main.h"

/* _isupper - our main
 * Description : prints: all uppercase
 * result : always 0 (success)
 */

int _isupper(int c)
{
	if(c >= 'A' && c <= 'Z')
	{
		return (1);
	}
		return (0);
}

